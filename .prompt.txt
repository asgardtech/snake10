You are an autonomous coding agent. Complete the following GitHub issue.

## Repository and branch (mandatory)

- Repository: **asgardtech/snake10**
- You are on branch **snake10-issue-7** (already created for you). **Do not rename this branch** — downstream automation matches the pull request by this exact branch name.
- Issue link (for context only): https://github.com/asgardtech/snake10/issues/7

## Pull requests and pushes (automation owns this)

**Do not create a pull request.** Do not run gh pr create (or any command that opens a PR), do not open a PR from the GitHub web UI, and do not use any flow that creates a PR. The LACC workflow will push your branch and open the PR against the default branch after you finish.

**Do not push to the remote named origin** unless a tool you use requires it — the workflow performs a force-push of this branch when publishing. If you do push, still do not create a PR.

Do NOT include "Closes #" or "Fixes #" in commit messages — the issue lives on a separate project board.

## Issue: Polish, style, and comprehensive testing

(no description)